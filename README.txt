To run the site locally:

1. Using the command line, navigate to the root directory of the project (recycle)
2. Execute command 'npm install'
3. Execute command 'node server.js'
4. In a web browser, navigate to 'http://localhost:8080/enter'